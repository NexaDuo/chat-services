# =============================================================================
# NexaDuo — Omnichannel AI Stack
# Aggregator file for local development.
# Uses the segmented files from the deploy/ directory.
# =============================================================================

include:
  - path: deploy/docker-compose.shared.yml
  - path: deploy/docker-compose.chatwoot.yml
  - path: deploy/docker-compose.dify.yml
  - path: deploy/docker-compose.nexaduo.yml
