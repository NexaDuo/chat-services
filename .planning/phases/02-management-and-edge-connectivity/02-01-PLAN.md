---
phase: 02-management-and-edge-connectivity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "infrastructure/terraform/modules/gcp-vm/main.tf",
  "infrastructure/terraform/envs/production/terraform.tfvars"
]
autonomous: true
requirements: ["INFRA-04", "ROUTE-04"]

must_haves:
  truths:
    - "Coolify v4 is running on the GCP VM"
    - "Admin account is initialized"
    - "GCP Firewall blocks all non-IAP ingress on port 22"
    - "GCP Firewall blocks all public ingress on port 3000/80/443 (after tunnel setup)"
  artifacts:
    - path: "infrastructure/terraform/modules/gcp-vm/main.tf"
      provides: "Hardened firewall rules"
  key_links:
    - from: "User Browser"
      to: "Coolify (Local IP)"
      via: "Direct connection (temporary for setup)"
---

<objective>
Verify Coolify v4 installation on the GCP VM and harden the instance security by restricting all ingress to the Google IAP (Identity-Aware Proxy) range for SSH.

Purpose: Establishes the management layer and secures the origin from public scanning.
Output: Initialized Coolify instance and a hardened firewall.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-management-and-edge-connectivity/02-RESEARCH.md
@infrastructure/terraform/modules/gcp-vm/main.tf
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Coolify Installation & Initialize Admin</name>
  <files>infrastructure/terraform/envs/production/terraform.tfvars</files>
  <action>
    - SSH into the VM (using gcloud compute ssh --tunnel-through-iap).
    - Check if coolify services are running: `docker ps`.
    - If not running, re-run installer: `curl -fsSL https://cdn.coollabs.io/coolify/install.sh | bash`.
    - Access Coolify via http://[VM_IP]:3000 (temporary firewall rule may be needed if not using SSH tunnel).
    - Initialize the first Admin account (email/password).
  </action>
  <verify>
    <automated>curl -f http://localhost:3000 | grep "Coolify"</automated>
  </verify>
  <done>Coolify dashboard is reachable and admin account is created.</done>
</task>

<task type="auto">
  <name>Task 2: Harden GCP Firewall for Portless Ingress</name>
  <files>infrastructure/terraform/modules/gcp-vm/main.tf</files>
  <action>
    - Update `infrastructure/terraform/modules/gcp-vm/main.tf` to remove any existing public ingress rules for 80, 443, or 3000.
    - Ensure ingress is ONLY allowed from the Google IAP IP range (35.235.240.0/20) on port 22.
    - Apply the changes: `terraform apply -auto-approve` in `infrastructure/terraform/envs/production`.
  </action>
  <verify>
    <automated>gcloud compute firewall-rules list --filter="name~'nexaduo'"</automated>
  </verify>
  <done>All public ingress is blocked; only SSH via IAP is permitted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Internet → GCP VM | Direct access is being removed in favor of Tunnels and IAP. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | Port 3000 | Mitigate | Restrict access via firewall until Tunnel is ready. |
| T-02-02 | Elevation of Privilege | SSH | Mitigate | Require IAP authentication for SSH access. |
</threat_model>

<success_criteria>
1. Coolify is running and responsive on the VM.
2. `terraform apply` reports zero public ingress rules remaining.
</success_criteria>

<output>
After completion, create `.planning/phases/02-management-and-edge-connectivity/02-01-SUMMARY.md`
</output>
