---
phase: "07-repository-hardening"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified: [".env.example", "edge/cloudflare-worker/wrangler.jsonc", "automation/initial-setup.js", "agents/self-healing/src/index.ts", "deploy/docker-compose.shared.yml", "deploy/docker-compose.chatwoot.yml", "deploy/docker-compose.dify.yml", "deploy/docker-compose.nexaduo.yml"]
autonomous: true
requirements: ["HARD-01", "HARD-02", "HARD-04"]

must_haves:
  truths:
    - "No hardcoded secrets remain in .env.example, wrangler.jsonc, or initial-setup.js"
    - "Services fail fast when critical environment variables are missing (no insecure fallbacks)"
    - "All Docker images are pinned to specific, immutable versions"
  artifacts:
    - path: ".env.example"
      provides: "Scrubbed environment template"
    - path: "edge/cloudflare-worker/wrangler.jsonc"
      provides: "Secure worker configuration"
    - path: "automation/initial-setup.js"
      provides: "Hardened automation script"
    - path: "agents/self-healing/src/index.ts"
      provides: "Hardened self-healing agent"
  key_links:
    - from: "deploy/docker-compose.chatwoot.yml"
      to: "chatwoot/chatwoot:v4.1.0"
      via: "image pinning"
---

<objective>
Remove hardcoded secrets from tracked files, eliminate insecure credential fallbacks, and pin all container images to specific versions to prepare the repository for public release.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/todos/pending/2026-04-16-harden-repo-for-public-github-security.md
</context>

<tasks>

<task type="auto">
  <name>Scrub secrets from config files</name>
  <files>.env.example, edge/cloudflare-worker/wrangler.jsonc, automation/initial-setup.js</files>
  <action>
    - In `.env.example`: Replace any real secret values with placeholders like `REPLACE_ME` (per HARD-01).
    - In `edge/cloudflare-worker/wrangler.jsonc`: Remove `SHARED_SECRET` from `vars` and document it as a required secret binding in the README or a comment.
    - In `automation/initial-setup.js`: Remove hardcoded `ADMIN_PASSWORD` fallback. Throw an error if `process.env.ADMIN_PASSWORD` is missing.
  </action>
  <verify>
    <automated>grep -E "28369644b8f8|NexaDuo@2025" .env.example edge/cloudflare-worker/wrangler.jsonc automation/initial-setup.js || echo "Secrets cleared"</automated>
  </verify>
  <done>Secrets removed from tracked files.</done>
</task>

<task type="auto">
  <name>Remove insecure credential fallbacks</name>
  <files>agents/self-healing/src/index.ts, automation/initial-setup.js</files>
  <action>
    - In `agents/self-healing/src/index.ts`: Remove the `postgresql://postgres:postgres@...` fallback. Require `DATABASE_URL` or individual `DB_*` vars to be present; throw Error if missing (per HARD-02).
    - In `automation/initial-setup.js`: Ensure `ADMIN_EMAIL` also requires an environment variable without a hardcoded fallback.
  </action>
  <verify>
    <automated>grep "postgres:postgres" agents/self-healing/src/index.ts || echo "Fallbacks removed"</automated>
  </verify>
  <done>Insecure fallbacks removed and fail-fast implemented.</done>
</task>

<task type="auto">
  <name>Pin container images in Docker Compose</name>
  <files>deploy/docker-compose.shared.yml, deploy/docker-compose.chatwoot.yml, deploy/docker-compose.dify.yml, deploy/docker-compose.nexaduo.yml</files>
  <action>
    - Update all `image:` tags to use specific versions instead of `latest` or generic tags (per HARD-04).
    - Use `pgvector/pgvector:pg16` (confirmed), `redis:7.2.4-alpine`, `alpine:3.19`, `ubuntu/squid:22.04-22.04_beta`.
    - Ensure `atendai/evolution-api:v2.1.1` and other existing pins are maintained or updated to current stable.
  </action>
  <verify>
    <automated>grep "image:" deploy/*.yml | grep -v "latest" || echo "All images pinned"</automated>
  </verify>
  <done>All container images pinned to specific versions.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | .env.example / wrangler.jsonc | mitigate | Remove hardcoded secrets from tracked files. |
| T-07-02 | Elevation of Privilege | automation/initial-setup.js | mitigate | Remove default admin password fallbacks. |
| T-07-03 | Supply Chain | docker-compose.yml | mitigate | Pin container images to specific versions. |
</threat_model>

<success_criteria>
- No hardcoded secrets in the specified files.
- Applications throw errors when required credentials are missing instead of using insecure defaults.
- No `:latest` image tags in any compose file.
</success_criteria>

<output>
After completion, create .planning/phases/07-repository-hardening/07-01-SUMMARY.md
</output>
