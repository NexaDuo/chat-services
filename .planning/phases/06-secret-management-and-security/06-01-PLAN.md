---
phase: 06-secret-management-and-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [deploy/docker-compose.infisical.yml, infrastructure/terraform/envs/production/main.tf]
autonomous: false
requirements: [VAULT-01, VAULT-04]
user_setup:
  - service: infisical
    why: "Secret management vault"
    dashboard_config:
      - task: "Create 'NexaDuo' project"
      - task: "Create 'Production' environment"
      - task: "Create a Service Token for Terraform"

must_haves:
  truths:
    - "Infisical is accessible via the VM's Tailscale IP"
    - "Infisical is running as a Coolify service"
  artifacts:
    - path: "deploy/docker-compose.infisical.yml"
      provides: "Infisical deployment configuration"
    - path: "infrastructure/terraform/envs/production/main.tf"
      provides: "Infisical service definition"
  key_links:
    - from: "Terraform"
      to: "Infisical"
      via: "Coolify provider"
---

<objective>
Deploy Infisical as the central secret management vault on the existing infrastructure, ensuring it is accessible only via Tailscale.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@infrastructure/terraform/envs/production/main.tf
@deploy/docker-compose.shared.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Infisical Docker Compose</name>
  <files>deploy/docker-compose.infisical.yml</files>
  <action>
    Create a Docker Compose file for Infisical.
    - Use the official `infisical/infisical` image.
    - Reuse the existing `nexaduo-network` to connect to the shared Postgres/Redis if possible, or include internal DB/Redis for isolation.
    - For simplicity and isolation, include a dedicated Postgres and Redis for Infisical in this compose.
    - Ensure ports are NOT published to 0.0.0.0, but to 127.0.0.1 or rely on Coolify's reverse proxy (which we will secure via Tailscale).
  </action>
  <verify>
    <automated>ls deploy/docker-compose.infisical.yml</automated>
  </verify>
  <done>Docker Compose file created</done>
</task>

<task type="auto">
  <name>Task 2: Define Infisical Service in Terraform</name>
  <files>infrastructure/terraform/envs/production/main.tf</files>
  <action>
    Add a new `coolify_service` for Infisical to `main.tf`.
    - Name: `nexaduo-vault`.
    - Use `deploy/docker-compose.infisical.yml`.
    - Configure necessary environment variables (ENCRYPTION_KEY, JWT_SECRET, etc.). Use placeholders or variables that will eventually be moved to the vault itself (bootstrap secrets).
    - Ensure it depends on the `shared` stack if it reuses the network.
  </action>
  <verify>
    <automated>grep "nexaduo-vault" infrastructure/terraform/envs/production/main.tf</automated>
  </verify>
  <done>Infisical service defined in Terraform</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Infisical stack deployment</what-built>
  <how-to-verify>
    1. Run `terraform apply` to deploy the Infisical stack.
    2. Access Infisical via the VM's Tailscale IP (you may need to configure the Coolify proxy to listen on the Tailscale interface or use a Tailscale-aware reverse proxy).
    3. Verify you can reach the Infisical setup page.
  </how-to-verify>
  <resume-signal>Infisical is up and reachable via Tailscale</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Internet -> Vault | BLOCKED. Vault must only be accessible via Tailscale. |
| Agent -> Vault | Trusted. Terraform uses a Service Token. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | Vault UI | mitigate | Restrict access to Tailscale network only. |
| T-06-02 | Elevation of Privilege | Service Token | mitigate | Use least-privilege service tokens for Terraform. |
</threat_model>

<verification>
1. `docker ps` on the VM shows infisical containers running.
2. `curl` from a Tailscale-connected machine to the Infisical port returns 200.
</verification>

<success_criteria>
Infisical is deployed and reachable, ready to store the project's secrets.
</success_criteria>

<output>
After completion, create `.planning/phases/06-secret-management-and-security/06-01-SUMMARY.md`
</output>
