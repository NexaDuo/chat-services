---
phase: 06-secret-management-and-security
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: [infrastructure/terraform/envs/production/main.tf, infrastructure/terraform/envs/production/providers.tf, infrastructure/terraform/envs/production/variables.tf]
autonomous: false
requirements: [VAULT-02, VAULT-03]
user_setup:
  - service: infisical
    why: "Migration of secrets"
    dashboard_config:
      - task: "Manually upload secrets from terraform.tfvars to Infisical 'Production' environment"

must_haves:
  truths:
    - "Terraform fetches secrets from Infisical during plan/apply"
    - "Local terraform.tfvars no longer contains sensitive values"
  artifacts:
    - path: "infrastructure/terraform/envs/production/providers.tf"
      provides: "Infisical provider configuration"
  key_links:
    - from: "Terraform Variables"
      to: "Infisical Data Source"
      via: "data.infisical_secrets"
---

<objective>
Migrate all secrets from `terraform.tfvars` to Infisical and update Terraform to fetch them dynamically, eliminating the need for sensitive local files.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-secret-management-and-security/06-01-SUMMARY.md
@infrastructure/terraform/envs/production/main.tf
@infrastructure/terraform/envs/production/variables.tf
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Infisical Provider</name>
  <files>infrastructure/terraform/envs/production/providers.tf</files>
  <action>
    Add the `infisical` provider to the Terraform configuration.
    - Define the provider with the `host` set to the Infisical Tailscale URL.
    - Use a `service_token` variable (which will be the only secret passed via env var or bootstrap tfvars).
  </action>
  <verify>
    <automated>grep "infisical" infrastructure/terraform/envs/production/providers.tf</automated>
  </verify>
  <done>Infisical provider configured</done>
</task>

<task type="auto">
  <name>Task 2: Replace Variable Values with Infisical Data Sources</name>
  <files>infrastructure/terraform/envs/production/main.tf</files>
  <action>
    - Add `data "infisical_secrets" "prod"` to fetch all secrets from the 'Production' environment.
    - Update all sensitive resource arguments (e.g., `POSTGRES_PASSWORD`, `CHATWOOT_SECRET_KEY_BASE`) to use `data.infisical_secrets.prod.secrets["KEY_NAME"].value` instead of `var.key_name`.
  </action>
  <verify>
    <automated>grep "data.infisical_secrets" infrastructure/terraform/envs/production/main.tf</automated>
  </verify>
  <done>Terraform resources updated to use vault secrets</done>
</task>

<task type="auto">
  <name>Task 3: Cleanup Local Secrets</name>
  <files>infrastructure/terraform/envs/production/variables.tf, infrastructure/terraform/envs/production/terraform.tfvars.example</files>
  <action>
    - Mark sensitive variables as optional or remove default values that hint at format.
    - Update `terraform.tfvars.example` to explain that secrets are now managed in Infisical.
    - Ensure the real `terraform.tfvars` is empty of sensitive data (executor will do this manually).
  </action>
  <verify>
    <automated>grep "Infisical" infrastructure/terraform/envs/production/terraform.tfvars.example</automated>
  </verify>
  <done>Local configuration sanitized</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Terraform + Infisical Integration</what-built>
  <how-to-verify>
    1. Ensure secrets are uploaded to Infisical.
    2. Run `terraform plan`.
    3. Verify that Terraform successfully fetches secrets and shows no changes (or only expected changes) to infrastructure.
  </how-to-verify>
  <resume-signal>Terraform is successfully integrated with Infisical</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Vault -> Terraform | Secure via Service Token and Tailscale. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | Secret Values | mitigate | Use Infisical's audit logs to track changes to secrets. |
| T-06-04 | Denial of Service | Vault Availability | accept | Vault is self-hosted; if down, Terraform apply will fail but services continue to run. |
</threat_model>

<verification>
`terraform plan` completes successfully without requiring a local sensitive `.tfvars` file.
</verification>

<success_criteria>
Secrets are fully migrated to Infisical and Terraform is fetching them dynamically.
</success_criteria>

<output>
After completion, create `.planning/phases/06-secret-management-and-security/06-02-SUMMARY.md`
</output>
