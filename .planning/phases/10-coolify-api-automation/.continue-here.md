---
context: phase
phase: 10-coolify-api-automation
task: null
total_tasks: 0
status: paused
last_updated: 2026-04-22T00:36:23.501Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: No CONTEXT.md yet — the plan-phase workflow's CONTEXT gate requires `/gsd-discuss-phase 10` to run as a top-level command. Nested invocation breaks AskUserQuestion (GSD issue #1009).
- [ ] CONSTRAINT: Provider ceiling is real — `SierraJC/coolify` v0.10.2 does NOT support `coolify_destination`, `coolify_project_environment`, health-check or deployment-setting attributes on `coolify_service`, and `coolify_application` is upstream-marked "Partial" with no public resource doc. Any plan that assumes these resources exist will fail at `terraform plan`.

**Do not proceed until both boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Planning against non-existent provider resources | Assuming `coolify_application`/`coolify_destination`/`health_check_*` exist in v0.10.2 | blocking | Read `10-RESEARCH.md` § "Accepted Gaps" before drafting any plan; only use resources listed under "Architecture Patterns". |
| Starting plan-phase with >70% context used | plan-phase spawns 2–3 subagents + up to 3 revision iterations; context at 90% at spawn time will be exhausted mid-loop | blocking | Run `/gsd-plan-phase 10` only in a fresh session after `/clear`. |

<current_state>
Session focus: finish the Gemini session `21fb507a-df26-4f2a-aaac-618aa3b351af` (sub-session `sdtc3p` — Phase 10 research) that was abandoned mid-task. Research file produced. Plan-phase workflow initialized but halted at the CONTEXT gate (no CONTEXT.md) with context budget at 90%.
</current_state>

<completed_work>

- Analyzed 5 sub-sessions of Gemini session `21fb507a` and identified `sdtc3p` (Phase 10 research) as the interrupted task.
- Audited the actual `SierraJC/coolify` v0.10.2 provider schema (resources `coolify_service`, `coolify_service_envs`, `coolify_project`; support matrix for Applications/Destinations/Environments).
- Cross-referenced current tenant layer (`infrastructure/terraform/envs/production/tenant/main.tf`, `providers.tf`) for actual secret-interpolation sites (14 secrets via `templatefile()` at main.tf:23-29, 52-58, 85-97, 124-136).
- Wrote `.planning/phases/10-coolify-api-automation/10-RESEARCH.md` with: provider ceiling, architectural pattern (layered declaration), migration targets, 6 common pitfalls, accepted gaps list, and suggested 4-plan decomposition (10-01 envs extraction → 10-02 bootstrap gap → 10-03 deploy trigger → 10-04 docs).
- Initialized `/gsd-plan-phase 10` — confirmed phase validates (INFRA-06), phase dir exists, research detected. Halted before spawning subagents due to 90% context.
</completed_work>

<remaining_work>

1. **`/gsd-discuss-phase 10`** (top-level, NOT nested) — capture locked decisions for Phase 10. Key questions the discuss-phase should surface:
   - Migration order across the 4 stacks (shared → chatwoot → dify → nexaduo, or all-at-once)?
   - `instant_deploy = true` with one-shot env race vs `instant_deploy = false` two-step (see RESEARCH.md Pitfall 6)?
   - Retire `FORCE_REDEPLOY_HASH` after envs migration, or keep as failsafe?
   - Scope of `scripts/bootstrap-coolify.sh` extension for destination-creation fallback?
   - Use `null_resource` + `POST /deploy` for per-env rotation trigger, or rely on `terraform apply -replace`?
2. **`/gsd-plan-phase 10`** in fresh session — will spawn `gsd-pattern-mapper`, `gsd-planner`, and `gsd-plan-checker` (up to 3 revision iterations). Research skip is automatic (`has_research: true`).
3. Execute produced plans via `/gsd-execute-phase 10`.
</remaining_work>

<decisions_made>

- **Keep `SierraJC/coolify` v0.10.2** — do not swap providers in Phase 10. Only viable community provider for Coolify v4.
- **Layered declaration is the target pattern** — `coolify_service` carries topology (compose file), `coolify_service_envs` carries runtime secrets. Do NOT split stacks into per-container resources (provider does not support it).
- **Explicitly document accepted gaps** (destination creation, project_environment, health checks, deploy triggers) as non-Terraform concerns rather than chase false declarativity.
- **Proceed despite `agents_installed: false`** — gsd-sdk init reports all 17 GSD subagents missing but they resolve via the central Skill tool. Confirmed during the preceding Gemini session that planner/checker runs succeeded (see `.planning/phases/09-*/09-*-PLAN.md`).
</decisions_made>

<blockers>
- **Missing CONTEXT.md for Phase 10**: the plan-phase gate requires running `/gsd-discuss-phase 10` as a top-level command. Cannot be invoked nested from plan-phase due to AskUserQuestion limitation.
- **Context budget at 90%**: plan-phase's subagent chain (pattern-mapper + planner + checker × up to 3 iterations) will exhaust context. Resume must happen in a fresh session.
</blockers>

## Required Reading (in order)

1. `.planning/phases/10-coolify-api-automation/10-RESEARCH.md` — Phase 10 technical research, section "Implementation Phasing" lists suggested plan decomposition (10-01 → 10-04).
2. `infrastructure/terraform/envs/production/tenant/main.tf:15-152` — current monolithic `coolify_service` + `templatefile()` pattern that Phase 10 refactors.
3. `infrastructure/terraform/envs/production/tenant/providers.tf:1-26` — pinned provider version (`SierraJC/coolify` 0.10.2).
4. `.planning/MILESTONES.md` v1.1 — "Key Goals" block mentioning 100% declarative Terraform provisioning for INFRA-06.
5. `.planning/REQUIREMENTS.md` — INFRA-06 definition with v1.0 vs v1.1 split.
6. `.planning/phases/09-architectural-infrastructure-refactor/09-CONTEXT.md` — upstream Phase 09 3-step deployment architecture (Foundation → Bootstrap → Applications) that Phase 10 MUST NOT regress.

## Critical Anti-Patterns (do NOT repeat)

- **Do not** assume `coolify_application`, `coolify_destination`, `coolify_project_environment`, or health-check attributes exist in provider v0.10.2 — they don't. Will fail schema validation.
- **Do not** split any stack into per-container Terraform resources — `coolify_service` represents one compose project, not a container.
- **Do not** wholesale-delete `FORCE_REDEPLOY_HASH` during envs migration — only safe to remove for secrets that actually moved out of the compose body.
- **Do not** drive `coolify_service_envs` from a list — use a map with `for_each` to get stable diffs.

## Infrastructure State

- **Phase 09 deployment**: 3-step Foundation → Bootstrap → Applications working per last commit `d848044 feat: finalize milestone v1.0 and close backlog gaps`.
- **Current tenant Terraform**: 4 monolithic `coolify_service` resources (shared, chatwoot, dify, nexaduo), each with ~3–7 secrets interpolated via `templatefile()`.
- **Branch**: `copilot/create-github-workflow-docker-compose` with ~15 files modified/staged (not relevant to Phase 10; cleanup TBD).
- **Dirty files**: see `git status` at session start — includes `deploy/docker-compose.*.yml`, Terraform files, `scripts/bootstrap-coolify.sh`, `.planning/*.md`.

## Pre-Execution Critique Required

Not applicable — pausing between research and plan-phase, not between design and execution.

<context>
The research was written to be plan-ready: every accepted gap is explicit, every claim is grounded in a verified provider doc page or codebase line reference. A resuming agent reading RESEARCH.md should be able to skip re-investigation and move straight into discuss-phase. The heavy lifting that remains is deciding the *order* of migration and the `instant_deploy` race strategy — both are surfaced as open questions for discuss-phase in the "Remaining Work" block.
</context>

<next_action>
Run `/gsd-discuss-phase 10` as a top-level command in a fresh session.
</next_action>
