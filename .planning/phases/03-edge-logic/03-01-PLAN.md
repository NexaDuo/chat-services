---
phase: 03-edge-logic
plan: 01
type: execute
wave: 1
depends_on: ["Phase 2"]
files_modified: []
autonomous: true
requirements: ["ROUTE-02", "ROUTE-03"]

must_haves:
  truths:
    - "Cloudflare Worker project is initialized with Hono"
    - "Worker can extract {tenant} from path /chat.nexaduo.com/{tenant}/*"
    - "Worker injects X-Tenant-ID header into outgoing requests"
  artifacts:
    - path: "edge/cloudflare-worker/src/index.ts"
      provides: "Routing and header injection logic"
---

<objective>
Initialize the Cloudflare Worker project and implement the core path-based routing and header injection logic using the Hono framework.

Purpose: Enables multi-tenant routing at the network edge as per the final architectural choice.
Output: Functional Cloudflare Worker code ready for deployment.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Cloudflare Worker Project</name>
  <action>
    - Create `edge/cloudflare-worker` directory.
    - Initialize a new Hono project: `npm create hono@latest edge/cloudflare-worker -- --template cloudflare-workers`.
    - Install dependencies.
  </action>
  <done>Worker project structure is created.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Path Routing & Header Injection</name>
  <files>edge/cloudflare-worker/src/index.ts</files>
  <action>
    - Define Hono routes for Chatwoot and Dify domains.
    - Implement middleware to extract tenant slug from the first path segment.
    - Implement path stripping (rewrite URL).
    - Inject `X-Tenant-ID` and correct `Host` header.
    - Handle WebSocket upgrades.
  </action>
  <done>Core Worker logic is implemented.</done>
</task>

</tasks>

<success_criteria>
1. Worker code compiles without errors.
2. Local tests confirm path stripping and header injection work as expected.
</success_criteria>
