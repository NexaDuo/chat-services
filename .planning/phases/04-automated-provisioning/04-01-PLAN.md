---
phase: 04-automated-provisioning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [provisioning/package.json, provisioning/tsconfig.json, provisioning/src/cli.ts, provisioning/src/db.ts, provisioning/src/api.ts, provisioning/tenants.json]
autonomous: true
requirements: [PROV-01]

must_haves:
  truths:
    - "CLI command 'register-tenant' accepts slug and account ID"
    - "Tenant record is successfully inserted into Middleware database"
    - "CLI confirms tenant availability via Middleware API call"
  artifacts:
    - path: "provisioning/src/cli.ts"
      provides: "Main entry point for the CLI"
    - path: "provisioning/tenants.json"
      provides: "State file for Terraform DNS automation"
  key_links:
    - from: "provisioning/src/cli.ts"
      to: "middleware.tenants"
      via: "pg client"
    - from: "provisioning/src/cli.ts"
      to: "middleware/resolve-tenant"
      via: "axios fetch"
---

<objective>
Implement a Node.js/TypeScript Provisioning CLI to automate the registration of new tenants in the Middleware database and validate their status via the Middleware API.

Purpose: Move away from manual database inserts and semi-automated shell scripts to a robust, type-safe CLI that ensures data consistency and verifies reachability.
Output: A functional `provision-tenant` CLI tool.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-automated-provisioning/04-RESEARCH.md
@middleware/src/handlers/tenant.ts
@infrastructure/postgres/01-init.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap Provisioning CLI Project</name>
  <files>provisioning/package.json, provisioning/tsconfig.json, provisioning/src/cli.ts, provisioning/tenants.json</files>
  <action>
    - Initialize a new Node.js project in the `provisioning/` directory.
    - Set up `package.json` with ESM support (`"type": "module"`) and scripts (`start`, `build`).
    - Install dependencies per RESEARCH.md: `commander`, `zod`, `pg`, `axios`, `dotenv`.
    - Install dev dependencies: `typescript`, `@types/node`, `@types/pg`.
    - Configure `tsconfig.json` for Node.js 20+ (ESNext).
    - Create a basic CLI entry point in `src/cli.ts` using `commander` that accepts a `register-tenant` command with `--slug` and `--account-id`.
    - Create an empty `tenants.json` array `[]`.
  </action>
  <verify>
    <automated>cd provisioning && npm start -- register-tenant --help</automated>
  </verify>
  <done>CLI project is scaffolded and command-line arguments are parsed correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Database Registration and State Update</name>
  <files>provisioning/src/cli.ts, provisioning/src/db.ts, provisioning/tenants.json</files>
  <action>
    - Create `src/db.ts` to handle PostgreSQL connection using `DATABASE_URL` from `.env`.
    - Implement the `register-tenant` logic in `src/cli.ts`:
      1. Validate input using a Zod schema (per PROV-01).
      2. Insert a new record into the `tenants` table in the `middleware` database.
      3. Fields to insert: `subdomain` (slug), `chatwoot_account_id`. Use default for others or allow optional flags.
      4. Update the local `provisioning/tenants.json` file by adding the new tenant object `{ "slug": slug, "accountId": accountId }`. This file will be used by Terraform in Plan 02.
  </action>
  <verify>
    <automated># Manual verification: check if record exists in DB after running CLI</automated>
  </verify>
  <done>Tenant is correctly persisted to the Middleware database and the local state file.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Middleware Validation</name>
  <files>provisioning/src/cli.ts, provisioning/src/api.ts</files>
  <action>
    - Create `src/api.ts` to wrap calls to the Middleware service.
    - After a successful database insert, the CLI must call: `GET /resolve-tenant?subdomain={slug}`.
    - Authenticate using `Authorization: Bearer ${HANDOFF_SHARED_SECRET}` from `.env`.
    - The CLI should print a success message if the Middleware returns the correct `accountId`, or an error if validation fails.
  </action>
  <verify>
    <automated># Simulation: Ensure CLI logs "Tenant validated successfully" when API returns 200</automated>
  </verify>
  <done>CLI successfully verifies that the Middleware can resolve the newly created tenant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CLI → DB | Direct connection to the database. Requires secure credentials. |
| CLI → Middleware | API call over network. Requires shared secret. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | .env file | mitigate | Use .gitignore and never commit credentials. |
| T-04-02 | Integrity | tenants.json | mitigate | Ensure JSON is valid and atomic updates are used if possible. |
| T-04-03 | Spoofing | CLI → Middleware | mitigate | Authenticate with HANDOFF_SHARED_SECRET. |
</threat_model>

<verification>
1. Run `npm start -- register-tenant --slug acme --account-id 123`.
2. Check `tenants` table: `SELECT * FROM tenants WHERE subdomain = 'acme';`.
3. Check `tenants.json` contains the entry.
4. Observe CLI log for "Tenant validated successfully".
</verification>

<success_criteria>
- CLI project exists with Node.js/TS.
- `register-tenant` command works and persists data to DB and JSON.
- Middleware validation pass ensures the system is ready for the new tenant.
</success_criteria>

<output>
After completion, create `.planning/phases/04-automated-provisioning/04-01-SUMMARY.md`
</output>
