---
phase: 04-automated-provisioning
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified: [scripts/verify-tenant.ts]
autonomous: true
requirements: [PROV-01, PROV-03]

must_haves:
  truths:
    - "New tenant is reachable at its dedicated path /slug/"
    - "Tenant identification header X-Tenant-ID is correctly resolved"
  artifacts:
    - path: "scripts/verify-tenant.ts"
      provides: "Automated E2E verification script"
---

<objective>
Implement an end-to-end verification script to automate the testing of newly provisioned tenants, ensuring correct routing, header injection, and service availability.

Purpose: Final validation of the automated provisioning process, bridging the CLI, database, and edge routing.
Output: A CLI tool to verify tenant reachability and configuration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-edge-logic/03-02-PLAN.md
@edge/cloudflare-worker/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement E2E Tenant Verification Script</name>
  <files>scripts/verify-tenant.ts</files>
  <action>
    - Create a TypeScript script to verify a tenant's end-to-end availability.
    - Requirements:
      1. Check DNS resolution for `{slug}.chat.nexaduo.com` (if PROV-02 was used).
      2. Perform an HTTP GET request to `https://chat.nexaduo.com/{slug}/`.
      3. Verify the response is 200 (or the expected Chatwoot/Dify landing page).
      4. If possible, call a "trace" or "health" endpoint that exposes the resolved `X-Tenant-ID` to confirm the Edge Worker is mapping correctly.
      5. Output a clear report: SUCCESS or FAILURE with reasons.
  </action>
  <verify>
    <automated># Run script against a known tenant: npx ts-node scripts/verify-tenant.ts --slug acme</automated>
  </verify>
  <done>Verification script successfully identifies routing or configuration errors for any tenant.</done>
  <status>completed</status>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Automated Provisioning Workflow (CLI + IaC + Verification)</what-built>
  <how-to-verify>
    1. Provision a new tenant: `provisioning/src/cli.ts register-tenant --slug test-tenant --account-id 999`.
    2. Commit and push the updated `tenants.json`.
    3. Run `terraform apply` to provision DNS.
    4. Wait for propagation.
    5. Run verification: `scripts/verify-tenant.ts --slug test-tenant`.
    6. Confirm everything passes.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
  <status>completed</status>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Internet → Edge Worker | Verification script acts as a client on the public internet. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Deny of Service | DNS Propagation | accept | Acknowledge that DNS propagation can take minutes; verification script should retry. |
</threat_model>

<verification>
1. Provision a dummy tenant and run the verification script.
2. Confirm the script detects a 404 for a non-existent tenant.
3. Confirm the script detects a 200 and correct routing for a valid tenant.
</verification>

<success_criteria>
- Verification script correctly identifies tenant status.
- End-to-end flow from CLI to public access is confirmed.
</success_criteria>

<output>
After completion, create `.planning/phases/04-automated-provisioning/04-03-SUMMARY.md`
</output>
