---
phase: 04-automated-provisioning
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: [infrastructure/terraform/modules/gcp-vm/main.tf, infrastructure/terraform/modules/cloudflare-dns/main.tf, infrastructure/terraform/envs/production/main.tf]
autonomous: true
requirements: [PROV-02, PROV-03]

must_haves:
  truths:
    - "GCP Firewall allows HTTP/HTTPS ingress only from Cloudflare IP ranges"
    - "Terraform creates CNAME records dynamically for each tenant in tenants.json"
  artifacts:
    - path: "infrastructure/terraform/modules/cloudflare-dns/main.tf"
      provides: "Dynamic DNS record creation"
  key_links:
    - from: "infrastructure/terraform/modules/cloudflare-dns/main.tf"
      to: "provisioning/tenants.json"
      via: "jsondecode(file(...))"
---

<objective>
Harden the origin infrastructure by restricting ingress to Cloudflare IP ranges and implement dynamic DNS record creation using Terraform to support newly provisioned tenants.

Purpose: Address T-03-01 by ensuring only Cloudflare (and the Edge Worker) can reach the origin, and automate DNS management for subdomains.
Output: Secure firewall configuration and automated DNS logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-edge-logic/03-02-PLAN.md
@infrastructure/terraform/modules/gcp-vm/main.tf
@infrastructure/terraform/modules/cloudflare-dns/main.tf
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restrict Origin Ingress to Cloudflare (T-03-01 Mitigation)</name>
  <files>infrastructure/terraform/modules/gcp-vm/main.tf</files>
  <action>
    - Add a new `google_compute_firewall` resource in `infrastructure/terraform/modules/gcp-vm/main.tf`.
    - Allow TCP ports 80 and 443.
    - Set `source_ranges` to the official Cloudflare IPv4 and IPv6 ranges.
    - Cloudflare IPs (static list or variable):
        IPv4: https://www.cloudflare.com/ips-v4
        IPv6: https://www.cloudflare.com/ips-v6
    - Apply the rule to instances with the `http-server` and `https-server` tags (or the VM's specific tag).
    - Remove or disable any existing firewall rule that allows `0.0.0.0/0` on HTTP/HTTPS.
  </action>
  <verify>
    <automated># terraform plan (observe addition of restricted rule and removal of open rule)</automated>
  </verify>
  <done>GCP Firewall is configured to block all non-Cloudflare traffic on web ports.</done>
  <status>completed</status>
</task>

<task type="auto">
  <name>Task 2: Implement Dynamic DNS for Tenants</name>
  <files>infrastructure/terraform/modules/cloudflare-dns/main.tf</files>
  <action>
    - Update the `cloudflare-dns` module to support a list of tenants.
    - Use `locals` and `jsondecode(file("${path.module}/../../../../provisioning/tenants.json"))` to read the tenant state.
    - Implement a `cloudflare_record` using `for_each` that iterates over the tenants list.
    - Each tenant should get a CNAME: `{slug}.chat.nexaduo.com` pointing to the main `chat.nexaduo.com` (proxied by Cloudflare).
    - Note: Path-based routing is primary, but subdomains provide flexibility for future features or custom domains.
  </action>
  <verify>
    <automated># terraform plan (observe creation of records for tenants in tenants.json)</automated>
  </verify>
  <done>Terraform dynamically provisions DNS records based on the provisioning CLI output.</done>
  <status>completed</status>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Internet → GCP VM | Public network interface. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing | Origin Header Injection | mitigate | Restrict ingress to Cloudflare IP ranges; only trust headers from known proxies. |
</threat_model>

<verification>
1. Run `terraform plan` in `infrastructure/terraform/envs/production/`.
2. Confirm the firewall source ranges match Cloudflare's published IPs.
3. Add a dummy entry to `provisioning/tenants.json` and verify `terraform plan` shows a new CNAME record.
</verification>

<success_criteria>
- Firewall rules successfully restrict traffic to Cloudflare IPs.
- CNAME records are automatically planned/applied based on the `tenants.json` file.
</success_criteria>

<output>
After completion, create `.planning/phases/04-automated-provisioning/04-02-SUMMARY.md`
</output>
