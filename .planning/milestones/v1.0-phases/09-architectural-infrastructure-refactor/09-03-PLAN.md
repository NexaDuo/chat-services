---
phase: 09-architectural-infrastructure-refactor
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - infrastructure/terraform/envs/production/tenant/main.tf
  - infrastructure/terraform/envs/production/tenant/providers.tf
  - infrastructure/terraform/envs/production/tenant/variables.tf
  - infrastructure/terraform/envs/production/tenant/backend.tf
  - infrastructure/terraform/envs/production/tenant/secrets.tf
  - infrastructure/terraform/envs/production/main.tf
  - infrastructure/terraform/envs/production/providers.tf
  - infrastructure/terraform/envs/production/variables.tf
  - infrastructure/terraform/envs/production/secrets.tf
  - infrastructure/terraform/envs/production/outputs.tf
  - .planning/codebase/ARCHITECTURE.md
autonomous: true
requirements: [INFRA-06, DEPLOY-01, DEPLOY-02, DEPLOY-03, DEPLOY-04]
must_haves:
  truths:
    - "Tenant layer provisions Coolify projects and services using the bootstrap-generated token."
    - "ARCHITECTURE.md accurately reflects the new 3-step deployment architecture."
    - "Root production Terraform directory is cleaned up and refactored into layers."
  artifacts:
    - path: "infrastructure/terraform/envs/production/tenant/main.tf"
      provides: "Coolify project and service orchestration"
    - path: "infrastructure/terraform/envs/production/tenant/providers.tf"
      provides: "Dynamic Coolify provider configuration"
  key_links:
    - from: "infrastructure/terraform/envs/production/tenant/providers.tf"
      to: "GCP Secret Manager"
      via: "data source"
---

<objective>
Move the application-level orchestration (Coolify projects and services) to an isolated "Tenant" Terraform layer, clean up the legacy monolithic root files, and update the project architecture documentation. This completes the separation of infrastructure from application configuration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@infrastructure/terraform/envs/production/main.tf
@infrastructure/terraform/envs/production/providers.tf
@infrastructure/terraform/envs/production/variables.tf
@infrastructure/terraform/envs/production/secrets.tf
@.planning/codebase/ARCHITECTURE.md
@.planning/phases/09-architectural-infrastructure-refactor/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Scaffold Tenant Layer</name>
  <files>
    infrastructure/terraform/envs/production/tenant/main.tf,
    infrastructure/terraform/envs/production/tenant/providers.tf,
    infrastructure/terraform/envs/production/tenant/variables.tf,
    infrastructure/terraform/envs/production/tenant/backend.tf,
    infrastructure/terraform/envs/production/tenant/secrets.tf
  </files>
  <action>
    Extract the tenant-related resources and move them into the `tenant/` subdirectory.
    
    1. **main.tf**: Move `coolify_project`, all `coolify_service`, `coolify_service_envs`, and `null_resource` (network creation, SQL upload).
    2. **providers.tf**: Configure `coolify` provider using `data.google_secret_manager_secret_version.coolify_url` and `data.google_secret_manager_secret_version.coolify_api_token`.
    3. **variables.tf**: Include variables required for service configuration (app_name, images, admin credentials, etc.).
    4. **secrets.tf**: Fetch all application secrets from Secret Manager (including the new `coolify_url`).
    5. **backend.tf**: Configure a separate GCS prefix for the tenant state (e.g., `terraform/state/tenant`).
  </action>
  <verify>
    <automated>cd infrastructure/terraform/envs/production/tenant && terraform init && terraform plan</automated>
  </verify>
  <done>Tenant layer is isolated and `terraform plan` succeeds using secrets from GCP.</done>
</task>

<task type="auto">
  <name>Clean up Legacy Root Files</name>
  <files>
    infrastructure/terraform/envs/production/main.tf,
    infrastructure/terraform/envs/production/providers.tf,
    infrastructure/terraform/envs/production/variables.tf,
    infrastructure/terraform/envs/production/secrets.tf,
    infrastructure/terraform/envs/production/outputs.tf
  </files>
  <action>
    Remove or refactor the root files in `infrastructure/terraform/envs/production/` that have been moved to `foundation/` and `tenant/`.
    The root directory can remain as a placeholder or be removed if all deployments are now directed to sub-directories. 
    Recommendation: Keep them as empty shells or remove them to avoid confusion, ensuring `backend.tf` is also handled.
  </action>
  <verify>
    <automated>ls infrastructure/terraform/envs/production/main.tf</automated>
  </verify>
  <done>Legacy monolithic files are removed or emptied.</done>
</task>

<task type="auto">
  <name>Update Architecture Documentation</name>
  <files>.planning/codebase/ARCHITECTURE.md</files>
  <action>
    Update `ARCHITECTURE.md` to reflect the new 3-step infrastructure process:
    
    1. **Step 1: Foundation Layer** (Terraform) - Provisioning VM, VPC, Cloudflare Tunnel.
    2. **Step 2: Bootstrap Step** (Script) - Installing Coolify, generating API token, and populating Secret Manager.
    3. **Step 3: Tenant Layer** (Terraform) - Orchestrating services via Coolify API.
    
    Explain why this separation was necessary (provider initialization timeouts).
  </action>
  <verify>
    <automated>grep -i "Bootstrap" .planning/codebase/ARCHITECTURE.md</automated>
  </verify>
  <done>ARCHITECTURE.md reflects the refactored infrastructure pattern.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Local -> Coolify API | Tenant orchestration via HTTP API |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Information Disclosure | Secret Manager fetching | mitigate | Ensure least-privileged IAM roles for Secret Manager access. |
| T-09-06 | Denial of Service | Coolify API Timeout | mitigate | Provider separation prevents Terraform lock-out due to API unavailability. |
</threat_model>

<success_criteria>
- `terraform plan` in the tenant directory succeeds.
- `ARCHITECTURE.md` is updated.
- Legacy files are cleaned up.
</success_criteria>

<output>
After completion, create `.planning/phases/09-architectural-infrastructure-refactor/09-03-SUMMARY.md`
</output>
