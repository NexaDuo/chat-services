---
phase: 09-architectural-infrastructure-refactor
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - scripts/bootstrap-coolify.sh
autonomous: true
requirements: [INFRA-04, VAULT-01, VAULT-03]
must_haves:
  truths:
    - "Bootstrap script successfully populates GCP Secret Manager with Coolify token and VM IP."
  artifacts:
    - path: "scripts/bootstrap-coolify.sh"
      provides: "Automated bridging between Foundation and Tenant layers"
---

<objective>
Refine the `bootstrap-coolify.sh` script to serve as the bridge between the Foundation and Tenant layers. It must reliably install Coolify, generate an API token, and store both the token and the VM's public IP in GCP Secret Manager to enable the Tenant layer's Terraform provider to initialize.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@scripts/bootstrap-coolify.sh
@.planning/STATE.md
@.planning/phases/09-architectural-infrastructure-refactor/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Refine Bootstrap Script</name>
  <files>scripts/bootstrap-coolify.sh</files>
  <action>
    Enhance `scripts/bootstrap-coolify.sh` with the following:
    
    1. **Robust Secret Creation**: Check if `coolify_api_token`, `coolify_destination_uuid`, and `coolify_url` secrets exist in GCP Secret Manager. Create them if they are missing before attempting to add versions.
    2. **Store Coolify URL**: Add a step to store the Coolify API endpoint (e.g., `http://{VM_IP}:8000/api/v1`) in a secret named `coolify_url`.
    3. **Pre-flight Checks**: Verify `gcloud` and `ssh` connectivity before starting the heavy lifting.
    4. **Improved Output**: Use clear status messages and hide sensitive tokens from logs.
  </action>
  <verify>
    <automated>bash -n scripts/bootstrap-coolify.sh</automated>
  </verify>
  <done>Bootstrap script is refined and ready to bridge foundation and tenant layers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Local Script -> GCP Secret Manager | Script pushing secrets to SSOT |
| Local Script -> VM (SSH) | Script executing commands on the origin |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Information Disclosure | Script Logs | mitigate | Ensure tokens are never printed to stdout/stderr in the script. |
| T-09-04 | Spoofing | SSH Connection | mitigate | Use `--tunnel-through-iap` to ensure connection to the correct VM via GCP IAM. |
</threat_model>

<success_criteria>
- Script passes syntax check.
- Script includes logic to create secrets if missing.
- Script handles the `coolify_url` secret.
</success_criteria>

<output>
After completion, create `.planning/phases/09-architectural-infrastructure-refactor/09-02-SUMMARY.md`
</output>
