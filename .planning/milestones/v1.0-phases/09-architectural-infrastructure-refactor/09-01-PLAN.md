---
phase: 09-architectural-infrastructure-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infrastructure/terraform/envs/production/foundation/main.tf
  - infrastructure/terraform/envs/production/foundation/providers.tf
  - infrastructure/terraform/envs/production/foundation/variables.tf
  - infrastructure/terraform/envs/production/foundation/outputs.tf
  - infrastructure/terraform/envs/production/foundation/backend.tf
  - infrastructure/terraform/envs/production/foundation/secrets.tf
autonomous: true
requirements: [INFRA-01, INFRA-02, INFRA-03, ROUTE-01, ROUTE-05]
must_haves:
  truths:
    - "Foundation layer provisions core GCP and Cloudflare resources without Coolify provider errors."
  artifacts:
    - path: "infrastructure/terraform/envs/production/foundation/main.tf"
      provides: "GCP VM, VPC, Storage and Cloudflare Tunnel resources"
    - path: "infrastructure/terraform/envs/production/foundation/outputs.tf"
      provides: "vm_public_ip and tunnel_token outputs"
  key_links:
    - from: "infrastructure/terraform/envs/production/foundation/main.tf"
      to: "infrastructure/terraform/modules/gcp-vm"
      via: "module"
---

<objective>
Isolate the core infrastructure components (GCP VM, VPC, Firewall, Storage, Cloudflare Tunnel, and DNS) into a separate Terraform workspace ("Foundation"). This prevents circular dependencies where the Coolify provider tries to validate against a VM that hasn't been created yet.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@infrastructure/terraform/envs/production/main.tf
@infrastructure/terraform/envs/production/providers.tf
@infrastructure/terraform/envs/production/variables.tf
@infrastructure/terraform/envs/production/secrets.tf
</context>

<tasks>

<task type="auto">
  <name>Scaffold Foundation Layer</name>
  <files>
    infrastructure/terraform/envs/production/foundation/main.tf,
    infrastructure/terraform/envs/production/foundation/providers.tf,
    infrastructure/terraform/envs/production/foundation/variables.tf,
    infrastructure/terraform/envs/production/foundation/outputs.tf,
    infrastructure/terraform/envs/production/foundation/backend.tf,
    infrastructure/terraform/envs/production/foundation/secrets.tf
  </files>
  <action>
    Extract the foundation-related resources from the root production files and move them into the `foundation/` subdirectory.
    
    1. **main.tf**: Move `module "vm"`, `module "dns_*"`, `module "backup_storage"`, and `module "tunnel"`.
    2. **providers.tf**: Include only `google` and `cloudflare` providers.
    3. **variables.tf**: Include only variables required by the moved modules (GCP project, region, zone, app_name, machine_type, disk_size, ssh_user, ssh_key, cloudflare_account_id, cloudflare_zone_id, base_domain).
    4. **outputs.tf**: Export `vm_public_ip` (from `module.vm.public_ip`) and `tunnel_token` (from `module.tunnel.tunnel_token`).
    5. **secrets.tf**: Include only the `cloudflare_api_token` data source.
    6. **backend.tf**: Configure a separate GCS prefix for the foundation state (e.g., `terraform/state/foundation`).
  </action>
  <verify>
    <automated>cd infrastructure/terraform/envs/production/foundation && terraform init && terraform plan</automated>
  </verify>
  <done>Foundation layer is isolated and `terraform plan` succeeds without provider initialization errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Local -> GCP/Cloudflare | Terraform provisioning core infrastructure |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | Foundation Outputs | mitigate | Use `sensitive = true` for `tunnel_token` output. |
| T-09-02 | Elevation of Privilege | GCS Backend | mitigate | Restrict GCS bucket access to the deployment service account. |
</threat_model>

<success_criteria>
- `terraform plan` in the foundation directory succeeds.
- No `coolify` provider is present in the foundation layer.
- Outputs correctly export the VM IP and Tunnel Token.
</success_criteria>

<output>
After completion, create `.planning/phases/09-architectural-infrastructure-refactor/09-01-SUMMARY.md`
</output>
