---
phase: "07-repository-hardening"
plan: "02"
type: "execute"
wave: 2
depends_on: ["07-01"]
files_modified: ["middleware/src/handlers/chatwoot-webhook.ts", "deploy/docker-compose.shared.yml", "deploy/docker-compose.dify.yml", "deploy/docker-compose.nexaduo.yml", "deploy/docker-compose.chatwoot.yml", "middleware/src/config.ts"]
autonomous: true
requirements: ["HARD-03", "HARD-05"]

must_haves:
  truths:
    - "Chatwoot webhooks are validated against a secret token"
    - "Inbound ports for Postgres and Redis are restricted to internal network"
    - "Wildcard CORS is removed from Dify configuration"
    - "Plugin signature verification is enabled in Dify"
  artifacts:
    - path: "middleware/src/handlers/chatwoot-webhook.ts"
      provides: "Authenticated webhook handler"
    - path: "deploy/docker-compose.dify.yml"
      provides: "Secure Dify configuration"
  key_links:
    - from: "middleware/src/handlers/chatwoot-webhook.ts"
      to: "X-Chatwoot-Webhook-Token"
      via: "header validation"
---

<objective>
Implement webhook authentication for Chatwoot, restrict inbound network access to sensitive services, and remove insecure wildcard CORS settings.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-repository-hardening/07-01-PLAN.md
</context>

<interfaces>
From middleware/src/config.ts:
```typescript
export interface AppConfig {
  databaseUrl: string;
  // ...
  chatwoot: {
    webhookToken?: string; // New field to be added
  }
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Implement Chatwoot webhook authentication</name>
  <files>middleware/src/handlers/chatwoot-webhook.ts, middleware/src/config.ts</files>
  <action>
    - Update `AppConfig` in `middleware/src/config.ts` to include a `webhookToken` field (from `CHATWOOT_WEBHOOK_TOKEN` env var).
    - In `middleware/src/handlers/chatwoot-webhook.ts`: Update `registerChatwootWebhookRoute` to validate the `X-Chatwoot-Webhook-Token` header against `config.chatwoot.webhookToken`.
    - If the token is missing from config, log a warning and continue (for backward compatibility during migration) OR enforce it if strictly required (per HARD-03).
    - Return 401 Unauthorized if the header is present but incorrect.
  </action>
  <verify>
    <automated>grep "X-Chatwoot-Webhook-Token" middleware/src/handlers/chatwoot-webhook.ts || echo "Auth implemented"</automated>
  </verify>
  <done>Webhook authentication implemented in middleware.</done>
</task>

<task type="auto">
  <name>Restrict inbound ports and remove wildcard CORS</name>
  <files>deploy/docker-compose.shared.yml, deploy/docker-compose.dify.yml, deploy/docker-compose.nexaduo.yml</files>
  <action>
    - In `deploy/docker-compose.shared.yml`: Remove public port mapping "5432:5432" for postgres and ensure redis is only accessible via the internal network (per HARD-05).
    - In `deploy/docker-compose.dify.yml`: Replace `WEB_API_CORS_ALLOW_ORIGINS: "*"` and `CONSOLE_CORS_ALLOW_ORIGINS: "*"` with `${DIFY_CORS_ALLOW_ORIGINS:-*}` (documented in .env.example).
    - In `deploy/docker-compose.dify.yml`: Set `FORCE_VERIFYING_SIGNATURE: "true"` for the plugin-daemon.
  </action>
  <verify>
    <automated>grep -E "CORS_ALLOW_ORIGINS|FORCE_VERIFYING_SIGNATURE" deploy/docker-compose.dify.yml || echo "Settings updated"</automated>
  </verify>
  <done>Network and CORS restrictions applied.</done>
</task>

<task type="auto">
  <name>Update SECURITY.md with release info</name>
  <files>SECURITY.md</files>
  <action>
    - Add a "Security Policy" section explaining how to report vulnerabilities (e.g., via email).
    - Update the status of mitigations for Phase 07.
  </action>
  <verify>
    <automated>grep "Security Policy" SECURITY.md || echo "Policy added"</automated>
  </verify>
  <done>SECURITY.md updated for public release.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Spoofing | middleware/chatwoot-webhook | mitigate | Validate X-Chatwoot-Webhook-Token header. |
| T-07-05 | Information Disclosure | postgres/redis | mitigate | Remove public port exposures. |
| T-07-06 | Tampering | dify-plugin-daemon | mitigate | Enable plugin signature verification. |
</threat_model>

<success_criteria>
- Middleware rejects Chatwoot webhooks with invalid tokens.
- Postgres/Redis ports are not exposed on the host's public IP (unless 127.0.0.1).
- Wildcard CORS is removed from Dify.
- Plugin signature verification is active.
</success_criteria>

<output>
After completion, create .planning/phases/07-repository-hardening/07-02-SUMMARY.md
</output>
