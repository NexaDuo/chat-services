# Secrets
.env
.env.*
!.env.example
!.env.production.example
.ssh/

# Local agent / Claude tooling
.agents/
# Version team-owned Claude assets (.claude/agents, .claude/skills) so future
# edits show up normally. Only keep local/env-specific state ignored:
.claude/settings.local.json
# Symlinks to locally-installed shared skills (point into .agents/; would be
# broken links on a fresh clone):
.claude/skills/cloud-run-basics
.claude/skills/create-github-action-workflow-specification
.claude/skills/review-and-refactor
.claude/skills/terraform-style-guide
.claude/skills/terraform-test
skills-lock.json
.antigravitycli/
.serena/

# Node
node_modules/
dist/
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Backups
backups/
*.sql.gz
# Generated SQL dumps at repo root — but keep versioned init scripts.
/*.sql

# Local volumes / runtime data
volumes/
data/
onboarding/*.png
onboarding/test-results/
onboarding/playwright-report/
test-results/
playwright-report/

# Logs
*.log
logs/

# OS / editors
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
*.tfvars
*.tfvars.json
override.tf
override.tf.json
_override.tf
_override.tf.json
.terraformrc
terraform.rc
# *.json # commented out to allow project files
!**/.planning/**/*.json
!**/dify-apps/*.yml
!**/.env.example
!**/*.tfvars.example
!**/package.json
!**/tsconfig.json
!**/tenants.json

# stray stderr-capture artifact (accidental in #124)
config-err.txt
